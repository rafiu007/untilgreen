{
  "name": "untilgreen-monorepo",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.13.2",
  "scripts": {
    "build": "pnpm -r --sort run build",
    "test": "pnpm -r --sort run test",
    "lint": "pnpm -r --sort run lint",
    "release": "pnpm build && pnpm test && pnpm -r publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9",
    "@types/node": "^20.14.0"
  }
}
